\documentclass[tikz, border = 1mm]{standalone}

\usepackage{xcolor}

\definecolor{myblue}{HTML}{377EB8}
\definecolor{myred}{HTML}{E41A1C}
\definecolor{myviolet}{HTML}{984EA3}
\definecolor{myorange}{HTML}{E66101}
\definecolor{mygreen}{HTML}{1B9E77}
\definecolor{mygold}{HTML}{B8860B}
\definecolor{myteal}{HTML}{008B8B}
\definecolor{mybrown}{HTML}{654321}

\usepackage{sansmathfonts}

% \usepackage{DejaVuSansMono} % Load DejaVu Sans Mono for monospaced sans-serif font
% \usepackage{amsmath} % For better math formatting
% \renewcommand{\sfdefault}{DejaVuSansMono-TLF} % Set DejaVuSansMono as default sans-serif font
% \renewcommand{\familydefault}{\sfdefault} % Set sans-serif as default
% \usepackage{sansmath} % Sans serif math
% \sansmath % Enable sans-serif math font globally

\begin{document}

\usetikzlibrary{
	er,
	arrows,
	positioning
}

\begin{tikzpicture}[
		auto,
		node distance = 30mm and .3mm,
		manifest0/.style = {
			rectangle,
			draw,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm,
            color = mygold
		},
		manifest1/.style = {
			rectangle,
			draw,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm,
            color = myteal
		},
		manifest2/.style = {
			rectangle,
			draw,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm,
            color = mybrown
		},
		epsiloncov/.style = {
			color = myorange,
			<->,
			>=stealth'
		},
		inv/.style = {
			rectangle,
			draw=none,
			fill=none,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm
		},
		latentwithin/.style = {
			ellipse,
			draw,
			ultra thick,
			densely dashed,
			color = violet,
			inner sep = 0pt,
			minimum size = 15mm,
			align = center,
		},
		latentwithinpath/.style = {
			->,
			ultra thick,
			densely dashed,
			color = violet,
			>=stealth',
		},
		latentwithincov/.style = {
			<->,
			color = violet,
			ultra thick,
			densely dashed,
			>=stealth',
		},
		epsilon/.style = {
			ellipse,
			draw,
			color = myorange,
			inner sep = 0pt,
			minimum size = 10mm,
			align = center,
			thick
		},
		epsilonpath/.style = {
			->,
			thick,
			color = myorange,
			>=stealth'
		},
		epsiloncov/.style = {
			<->,
			draw,
			thick,
			color = myorange,
			>=stealth'
		},
		latentbetween/.style = {
			ellipse,
			draw,
			dashdotted,
			color = gray,
			inner sep = 0pt,
			minimum size = 15mm,
			align = center,
		},
		latentbetweenpath/.style = {
			->,
			dashdotted,
			color = gray,
			>=stealth',
		},
		latentbetweencov/.style = {
			<->,
			dashdotted,
			color = gray,
			>=stealth',
		},
		latentintercept/.style = {
			ellipse,
			draw,
			dashdotted,
			color = mygreen,
			inner sep = 0pt,
			minimum size = 10mm,
			align = center,
		},
		latentinterceptpath/.style = {
			->,
			dashdotted,
			color = mygreen,
			>=stealth',
		},
		latentinterceptcov/.style = {
			<->,
			dashdotted,
			color = mygreen,
			>=stealth',
		},
		mean/.style={
			regular polygon,
			regular polygon sides = 3,
			draw,
			thick,
			inner sep = 0pt,
			minimum width = 7mm,
			minimum height = 7mm,
		},
		cov/.style = {
			<->,
			thick,
			>=stealth',
		},
	]
	% indicators
	\node[manifest0] (peak0) {$Y_{\mathrm{peak}_{t_{0}}}$};
	\node[inv] (top0) [right = of peak0] {};
	\node[manifest0] (rise0) [right = of top0] {$Y_{\mathrm{rise}_{t_{0}}}$};
	\node[inv] (center0) [right = of rise0] {};
	\node[manifest0] (fall0) [right = of center0] {$Y_{\mathrm{fall}_{t_{0}}}$};
	\node[inv] (bottom0) [right = of fall0] {};
	\node[manifest0] (dura0) [right = of bottom0] {$Y_{\mathrm{dura}_{t_{0}}}$};
	\node[inv] (pad0) [right = of dura0] {};
	\node[manifest1] (peak1) [right = of pad0] {$Y_{\mathrm{peak}_{t_{1}}}$};
	\node[inv] (top1) [right = of peak1] {};
	\node[manifest1] (rise1) [right = of top1] {$Y_{\mathrm{rise}_{t_{1}}}$};
	\node[inv] (center1) [right = of rise1] {};
	\node[manifest1] (fall1) [right = of center1] {$Y_{\mathrm{fall}_{t_{1}}}$};
	\node[inv] (bottom1) [right = of fall1] {};
	\node[manifest1] (dura1) [right = of bottom1] {$Y_{\mathrm{dura}_{t_{1}}}$};
	% within
	\node[latentwithin] (f0) [above = of center0] {$\mathrm{State}_{\mathrm{intoxication}_{t_{0}}}$};
	\node[latentwithin] (f1) [above = of center1] {$\mathrm{State}_{\mathrm{intoxication}_{t_{1}}}$};
	\draw[latentwithinpath] (f0) to node[near end, right] {$1$} (peak0);
	\draw[latentwithinpath] (f1) to node[near end, right] {$1$} (peak1);
	\draw[latentwithinpath] (f0) to node[near end, right] {$\lambda_{S_{\mathrm{rise}}}$} (rise0);
	\draw[latentwithinpath] (f1) to node[near end, right] {$\lambda_{S_{\mathrm{rise}}}$} (rise1);
	\draw[latentwithinpath] (f0) to node[near end, right] {$\lambda_{S_{\mathrm{fall}}}$} (fall0);
	\draw[latentwithinpath] (f1) to node[near end, right] {$\lambda_{S_{\mathrm{fall}}}$} (fall1);
	\draw[latentwithinpath] (f0) to node[near end, right] {$\lambda_{S_{\mathrm{dura}}}$} (dura0);
	\draw[latentwithinpath] (f1) to node[near end, right] {$\lambda_{S_{\mathrm{dura}}}$} (dura1);
	\draw[latentwithincov] (f0) to[out = 70, in = 110, looseness = 4] node[above] {$\psi_{S_{t_{0}}}$} (f0);
	\draw[latentwithincov] (f1) to[out = 70, in = 110, looseness = 4] node[above] {$\psi_{S_{t_{1}}}$} (f1);
	% between
	\node[inv] (fbpad) [above = of pad0] {};
	\node[latentbetween] (fb) [above = of fbpad] {$\mathrm{Trait}_{\mathrm{intoxication}}$};
	\draw[latentbetweenpath] (fb) to node[near start, right] {$1$} (peak0);
	\draw[latentbetweenpath] (fb) to node[near start, right] {$1$} (peak1);
	\draw[latentbetweenpath] (fb) to node[near start, right] {$\lambda_{T_{\mathrm{rise}}}$} (rise0);
	\draw[latentbetweenpath] (fb) to node[near start, right] {$\lambda_{T_{\mathrm{rise}}}$} (rise1);
	\draw[latentbetweenpath] (fb) to node[near start, right] {$\lambda_{T_{\mathrm{fall}}}$} (fall0);
	\draw[latentbetweenpath] (fb) to node[near start, right] {$\lambda_{T_{\mathrm{fall}}}$} (fall1);
	\draw[latentbetweenpath] (fb) to node[near start, right] {$\lambda_{T_{\mathrm{dura}}}$} (dura0);
	\draw[latentbetweenpath] (fb) to node[near start, right] {$\lambda_{T_{\mathrm{dura}}}$} (dura1);
	\draw[latentbetweencov] (fb) to[out = 70, in = 110, looseness = 4] node[above] {$\psi_{T}$} (fb);
	% epsilon
	\node[epsilon] (epsilon10) [below = of peak0] {$\varepsilon_{\mathrm{peak}_{t_{0}}}$};
	\node[epsilon] (epsilon20) [below = of rise0] {$\varepsilon_{\mathrm{rise}_{t_{0}}}$};
	\node[epsilon] (epsilon30) [below = of fall0] {$\varepsilon_{\mathrm{fall}_{t_{0}}}$};
	\node[epsilon] (epsilon40) [below = of dura0] {$\varepsilon_{\mathrm{dura}_{t_{0}}}$};
	\node[epsilon] (epsilon11) [below = of peak1] {$\varepsilon_{\mathrm{peak}_{t_{1}}}$};
	\node[epsilon] (epsilon21) [below = of rise1] {$\varepsilon_{\mathrm{rise}_{t_{1}}}$};
	\node[epsilon] (epsilon31) [below = of fall1] {$\varepsilon_{\mathrm{fall}_{t_{1}}}$};
	\node[epsilon] (epsilon41) [below = of dura1] {$\varepsilon_{\mathrm{dura}_{t_{1}}}$};
	\draw[epsilonpath] (epsilon10) to node[right] {$1$} (peak0);
	\draw[epsilonpath] (epsilon20) to node[right] {$1$} (rise0);
	\draw[epsilonpath] (epsilon30) to node[right] {$1$} (fall0);
	\draw[epsilonpath] (epsilon40) to node[right] {$1$} (dura0);
	\draw[epsilonpath] (epsilon11) to node[right] {$1$} (peak1);
	\draw[epsilonpath] (epsilon21) to node[right] {$1$} (rise1);
	\draw[epsilonpath] (epsilon31) to node[right] {$1$} (fall1);
	\draw[epsilonpath] (epsilon41) to node[right] {$1$} (dura1);
	\draw[epsiloncov] (epsilon10) to[out = -70, in = -110, looseness = 4] node[below] {$\theta_{\mathrm{peak}}$} (epsilon10);
	\draw[epsiloncov] (epsilon11) to[out = -70, in = -110, looseness = 4] node[below] {$\theta_{\mathrm{peak}}$} (epsilon11);
	\draw[epsiloncov] (epsilon20) to[out = -70, in = -110, looseness = 4] node[below] {$\theta_{\mathrm{rise}}$} (epsilon20);
	\draw[epsiloncov] (epsilon21) to[out = -70, in = -110, looseness = 4] node[below] {$\theta_{\mathrm{rise}}$} (epsilon21);
	\draw[epsiloncov] (epsilon30) to[out = -70, in = -110, looseness = 4] node[below] {$\theta_{\mathrm{fall}}$} (epsilon30);
	\draw[epsiloncov] (epsilon31) to[out = -70, in = -110, looseness = 4] node[below] {$\theta_{\mathrm{fall}}$} (epsilon31);
	\draw[epsiloncov] (epsilon40) to[out = -70, in = -110, looseness = 4] node[below] {$\theta_{\mathrm{dura}}$} (epsilon40);
	\draw[epsiloncov] (epsilon41) to[out = -70, in = -110, looseness = 4] node[below] {$\theta_{\mathrm{dura}}$} (epsilon41);
	% intercepts
	\node[inv] (upad0) [below = of top0] {};
	\node[inv] (upad1) [below = of bottom0] {};
	\node[inv] (upad2) [below = of top1] {};
	\node[inv] (upad3) [below = of bottom1] {};
	\node[latentintercept] (u1) [below = of upad0] {$\mathrm{Unique}_{\mathrm{peak}}$};
	\node[latentintercept] (u2) [below = of upad1] {$\mathrm{Unique}_{\mathrm{rise}}$};
	\node[latentintercept] (u3) [below = of upad2] {$\mathrm{Unique}_{\mathrm{fall}}$};
	\node[latentintercept] (u4) [below = of upad3] {$\mathrm{Unique}_{\mathrm{dura}}$};
	\draw[latentinterceptpath] (u1) to node[near start, above] {$1$} (peak0);
	\draw[latentinterceptpath] (u1) to node[near start, above] {$1$} (peak1);
	\draw[latentinterceptpath] (u2) to node[near start, above] {$1$} (rise0);
	\draw[latentinterceptpath] (u2) to node[near start, above] {$1$} (rise1);
	\draw[latentinterceptpath] (u3) to node[near start, above] {$1$} (fall0);
	\draw[latentinterceptpath] (u3) to node[near start, above] {$1$} (fall1);
	\draw[latentinterceptpath] (u4) to node[near start, above] {$1$} (dura0);
	\draw[latentinterceptpath] (u4) to node[near start, above] {$1$} (dura1);
	\draw[latentinterceptcov] (u1) to[out = -70, in = -110, looseness = 4] node[below] {$\psi_{\mathrm{peak}}$} (u1);
	\draw[latentinterceptcov] (u2) to[out = -70, in = -110, looseness = 4] node[below] {$\psi_{\mathrm{rise}}$} (u2);
	\draw[latentinterceptcov] (u3) to[out = -70, in = -110, looseness = 4] node[below] {$\psi_{\mathrm{fall}}$} (u3);
	\draw[latentinterceptcov] (u4) to[out = -70, in = -110, looseness = 4] node[below] {$\psi_{\mathrm{dura}}$} (u4);
\end{tikzpicture}

\end{document}
