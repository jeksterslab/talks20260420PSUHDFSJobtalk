\documentclass[tikz, border = 1mm]{standalone}

\usepackage{xcolor}

\definecolor{direct}{HTML}{2C7BB6}
\definecolor{indirect}{HTML}{D7191C}
\definecolor{total}{HTML}{5E3C99}

\usepackage{sansmathfonts}

\begin{document}

\usetikzlibrary{
	er,
	arrows,
	positioning
}

\begin{tikzpicture}[
		auto,
		node distance = 20mm,
		manifest/.style = {
			rectangle,
			draw,
			thick,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm
		},
		inv/.style = {
			rectangle,
			draw=none,
			fill=none,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm
		},
		error/.style = {
			ellipse,
			draw,
			thick,
			inner sep = 0pt,
			minimum size = 7mm,
			align = center
		},
		mean/.style={
			regular polygon,
			regular polygon sides = 3,
			draw,
			thick,
			inner sep = 0pt,
			minimum width = 7mm,
			minimum height = 7mm
		},
		path/.style = {
			->,
			ultra thick,
			>=stealth'
		},
		dot/.style = {
			->,
			thick,
			>=stealth',
            densely dotted
		},
		dashed/.style = {
			->,
			thick,
			>=stealth',
            loosely dashed
		},
		cov/.style = {
			<->,
			thick,
			>=stealth'
		},
	]
	\node[manifest] (X) {$X_{\mathrm{wave} \ 1}$};
	\node[manifest] (M) [below right = of X] {$M_{\mathrm{wave} \ 2}$};
	\node[manifest] (Y) [above right = of M] {$Y_{\mathrm{wave} \ 3}$};
	\draw[path, color = indirect] (M) to node {$\beta$} (Y);
	\draw[dot, color = direct] (X) to node {$\tau^{\prime}$} (Y);
	\draw[path, color = indirect] (X) to node {$\alpha$} (M);
\end{tikzpicture}

\end{document}
